//
// Created by Connor DePalma on 10/11/18.
//

#include <vector>
#include <list>

int main(){
    vector<int> a;
    for(int i = o; i<10; i++)
        a.push_back(i);
    
    for(vector<int>::iterator i = a.begin(); i != a.end(); ++i)
        cout << i;
}